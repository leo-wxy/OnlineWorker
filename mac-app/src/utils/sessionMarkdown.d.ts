import type { ReactElement } from "react";

export const sessionMarkdownComponents: Record<string, unknown>;

export function SessionMarkdownContent(props: { content: string }): ReactElement;

export function renderSessionMarkdownToStaticMarkup(content: string): string;
