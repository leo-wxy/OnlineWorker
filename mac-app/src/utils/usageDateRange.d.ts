import type { UsageQuery } from "../types";

export const DEFAULT_RANGE_DAYS: number;

export function localIsoDate(date: Date): string;

export function buildDefaultUsageQuery(baseDate?: Date): UsageQuery;
