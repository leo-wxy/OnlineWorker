import type { ProviderMetadata } from "../types";

export function visibleSessionProviders(providers: ProviderMetadata[]): ProviderMetadata[];

export function visibleSessionProviderIds(providers: ProviderMetadata[]): string[];
