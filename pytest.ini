[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    allow_missing_data_dir: disable the default test data-dir sandbox for tests that must observe a missing data dir
