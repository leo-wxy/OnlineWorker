---
phase: 01-ui-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mac-app/src/index.css
  - mac-app/src/App.tsx
requirements:
  - UI-01
  - UI-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - 左上角虚线框从正式界面中消失
    - 去掉虚线框后仍保留窗口拖拽能力
    - 壳层样式变化不会破坏顶层布局和主要页面切换
  artifacts:
    - mac-app/src/index.css
    - mac-app/src/App.tsx
  key_links:
    - drag 区域的视觉样式与 Tauri 拖拽行为被明确解耦
---

<objective>
清理左上角虚线 drag strip，并把应用壳层相关样式收口到更干净的一致基线。

Purpose: 去掉最明显的视觉噪音，同时保证 Tauri 桌面窗口交互不回退。
Output: 更干净的拖拽区样式和收缩态 sidebar 所需的共享样式补充。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-foundation/01-CONTEXT.md
@.planning/phases/01-ui-foundation/01-RESEARCH.md
@mac-app/src/index.css
@mac-app/src/App.tsx
@mac-app/src-tauri/tauri.conf.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 去掉 drag strip 的虚线框视觉但保留拖拽热区</name>
  <files>mac-app/src/index.css, mac-app/src/App.tsx</files>
  <read_first>mac-app/src/index.css, mac-app/src/App.tsx, .planning/phases/01-ui-foundation/01-CONTEXT.md</read_first>
  <action>清理 `ow-drag-strip` 当前的虚线边框与显眼占位视觉。若保留该节点，就让它回退成更隐形的热区；若合并到其他壳层节点，也必须保留 `data-tauri-drag-region` 和现有拖拽入口。不要为了视觉清理而删掉窗口拖拽能力。</action>
  <verify>rg -n "ow-drag-strip|data-tauri-drag-region|startDragging" mac-app/src/App.tsx mac-app/src/index.css</verify>
  <acceptance_criteria>
    - 不再存在带虚线框的 drag strip 视觉
    - 拖拽区仍与 Tauri 窗口拖拽逻辑连接
  </acceptance_criteria>
  <done>drag strip 的视觉噪音已移除且拖拽逻辑保留</done>
</task>

<task type="auto">
  <name>Task 2: 为 sidebar 收缩态补共享样式并控制风险范围</name>
  <files>mac-app/src/index.css, mac-app/src/App.tsx</files>
  <read_first>mac-app/src/index.css, mac-app/src/App.tsx, .planning/phases/01-ui-foundation/01-RESEARCH.md</read_first>
  <action>在 `index.css` 里补充少量针对 sidebar 收缩态需要的共享类或样式规则，优先复用现有 `ow-sidebar`、`ow-tab-button` 等类，不引入整套新视觉系统。确保 main surface、banner 和各 tab 内容区的顶层 flex/overflow 约束不被 sidebar 样式调整破坏。</action>
  <verify>rg -n "ow-sidebar|ow-tab-button|ow-drag-strip|ow-main-surface" mac-app/src/index.css</verify>
  <acceptance_criteria>
    - 收缩态所需样式能在共享层表达清楚
    - 没有把局部调整扩大成全局主题重写
    - 主内容容器的 `flex` / `overflow` 约束仍保持稳定
  </acceptance_criteria>
  <done>壳层共享样式已经能支撑 sidebar 收缩态与 drag 区清理</done>
</task>

<task type="auto">
  <name>Task 3: 做页面级回归验证并记录安装态仍需补验的边界</name>
  <files>mac-app/src/index.css, mac-app/src/App.tsx</files>
  <read_first>README.md, deploy/BUILD.md</read_first>
  <action>完成改动后，至少跑前端构建验证；如果可行，再做一次本地 UI 冒烟检查，重点看 `Dashboard`、`Setup`、`Sessions`、`Commands` 的壳层切换。若本轮只做到源码态验证，需要在总结里明确安装态拖拽体验仍待补验。</action>
  <verify>cd mac-app && npm run build</verify>
  <acceptance_criteria>
    - 前端 build 通过
    - 壳层改动后的主要 tab 仍可正常切换
    - 若未做安装态验证，风险被明确记录
  </acceptance_criteria>
  <done>drag 区和共享壳层样式完成最小回归验证</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd mac-app && npm run build` 通过
- [ ] 视觉上不再出现左上角虚线框
- [ ] 拖拽热区逻辑仍可追溯到现有 Tauri 窗口拖拽实现
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 左上角虚线框被移除
- sidebar 收缩态所需的壳层样式有明确共享收口

</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-foundation/01-02-SUMMARY.md`
</output>
