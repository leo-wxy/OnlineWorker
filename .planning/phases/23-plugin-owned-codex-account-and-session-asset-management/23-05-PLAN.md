---
phase: 23
plan: 5
title: 固化 Codex 账号模型与 Cockpit 兼容格式
type: execute
wave: 1
depends_on: []
requirements: [D-08, D-09, D-14, D-15, D-17, D-18, D-19, D-20, D-21, D-24, D-25]
files_modified:
  - plugins/providers/builtin/codex/python/account_model.py
  - plugins/providers/builtin/codex/python/compat.py
  - plugins/providers/builtin/codex/tests/test_account_compat.py
  - plugins/providers/builtin/codex/tests/test_account_model.py
autonomous: true
must_haves:
  truths:
    - "Token、agentIdentity、API key 三种 credential shape 与 object/array Cockpit 输入均可结构校验，local file import 使用同一 parser。"
    - "稳定身份用于 dedupe/upsert；重复导入更新原记录并保留 unknown fields，无法解析身份按 item 拒绝。"
    - "Cockpit commit 35963163813d7424b63cd6053874ce5fc7973d03 的 object/array/credential/unknown-field round-trip 不静默丢失字段。"
    - "普通 list DTO 只含 redacted identity/source/current/external state，不包含 credential。"
  artifacts:
    - path: plugins/providers/builtin/codex/python/compat.py
      provides: "Cockpit-compatible parse/export and raw unknown preservation"
    - path: plugins/providers/builtin/codex/python/account_model.py
      provides: "stable identity, upsert, redacted DTO"
  key_links:
    - "本计划不声明尚不存在的 frontend/backend entry；plugin.yaml feature declaration 延后到 23-10 与真实 entry 同时落地。"
    - "compat.py 的 raw object/extra 与 account_model 的 canonical record 供 23-06 store、23-08 apply/export 使用。"
---

<objective>
独立实现 Codex 账号数据模型与 Cockpit Tools 格式兼容，先固定四种导入源和 identity/upsert 语义，再交给加密存储和 action 层。

Purpose: 账号 transfer 的正确性来自 commit-pinned JSON shape 与 unknown-field round-trip，而不是视觉相似。
Output: Codex compat/model modules 与 synthetic fixture tests；manifest declaration 留到 23-10 与真实 entries 同时提交。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-CONTEXT.md
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-RESEARCH.md:121-170, 218-250
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-PATTERNS.md:7-28
@plugins/providers/builtin/codex/plugin.yaml
@plugins/providers/builtin/codex/python/provider.py:61-128

<interfaces>
`compat.py` must expose parse/export functions for `cockpit_tools` object-or-array input and per-item results. Required modes: regular token (`id_token`, `access_token`, `refresh_token`, `account_id`, `last_refresh`, `email`, `type`, `expired`), `agentIdentity` (`agent_identity`, `account_id`, `user_id`, `email`, `type`), and `apikey` (`OPENAI_API_KEY`, `email`, optional provider fields).
`account_model.py` must expose deterministic identity key, upsert/update result, external match classification, and a secret-free index DTO.
</interfaces>
</context>

<tasks>
<task id="23-05-01" type="auto" tdd="true">
  <name>先建立 Cockpit shape 与 identity/upsert fixtures</name>
  <files>plugins/providers/builtin/codex/tests/test_account_compat.py, plugins/providers/builtin/codex/tests/test_account_model.py</files>
  <behavior>
    - Test 1: object input is one record, array input is many records, unsupported top-level shape/version is explicit rejection.
    - Test 2: token, agentIdentity, and apikey preserve their distinct required/omitted fields.
    - Test 3: unknown top-level/nested fields survive import→model→export; stable identity re-import updates in place and redacted DTO has no secret.
    - Test 4: current effective auth identity classifies as matched or unmanaged without auto-import/apply.
  </behavior>
  <read_first>23-CONTEXT.md: D-08-D-21, 23-RESEARCH.md:121-170, 218-250, 23-VALIDATION.md:70-86, /tmp/cockpit-tools-root-3596316/src/utils/codexExportFormats.ts (fixture facts only)</read_first>
  <action>[D-08, D-09, D-14, D-15, D-17, D-18, D-19, D-20, D-21, D-24, D-25] Write synthetic Cockpit commit-pinned fixtures, including nested unknown fields and secret absence assertions, before implementation. Do not copy Cockpit source; assert independent behavior and per-item accepted/skipped/rejected/ambiguous results. Include OAuth and local-file outputs as model inputs while keeping network out of structural parsing.</action>
  <acceptance_criteria>Fixtures distinguish every supported credential mode and prove object/array compatibility, unknown-field preservation, stable upsert, external matched/unmanaged, and secret-free DTO requirements.</acceptance_criteria>
  <verify><automated>python3 -m pytest plugins/providers/builtin/codex/tests/test_account_compat.py plugins/providers/builtin/codex/tests/test_account_model.py -q</automated></verify>
  <done>Parser/model red tests capture exact Cockpit and identity contracts.</done>
</task>
<task id="23-05-02" type="auto">
  <name>实现 Codex 独立 compat/model</name>
  <files>plugins/providers/builtin/codex/python/account_model.py, plugins/providers/builtin/codex/python/compat.py</files>
  <read_first>plugins/providers/builtin/codex/plugin.yaml, plugins/providers/builtin/codex/python/provider.py, 23-RESEARCH.md:121-170, 218-250</read_first>
  <action>[D-08, D-09, D-14, D-15, D-17, D-18, D-19, D-20, D-21, D-24, D-25] Implement Codex-owned parse/normalize/export for object-or-array Cockpit shapes and the three credential modes plus local file content; retain raw unknown fields, validate required identity/fields/version, derive deterministic identity keys, upsert in place, classify external matched/unmanaged, and emit redacted index DTOs. Do not modify `plugin.yaml` until 23-10 creates both backend and frontend entries; keep implementation independent from Cockpit source and all OnlineWorker runtime/live surfaces.</action>
  <acceptance_criteria>All fixtures pass; regular token export stays credential-complete, agentIdentity omits fake access token, API key retains `OPENAI_API_KEY`, one record exports as Cockpit array, and unknown fields survive supported round-trip.</acceptance_criteria>
  <verify><automated>python3 -m pytest plugins/providers/builtin/codex/tests/test_account_compat.py plugins/providers/builtin/codex/tests/test_account_model.py -q &amp;&amp; git diff --check</automated></verify>
  <done>Codex model/compat layer is ready for encrypted store and explicit action flows.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Cockpit JSON/file → compat parser | External account shape and unknown fields enter plugin. |
| parser/model → index DTO | Secrets must be removed before list/UI boundary. |
| effective auth inspection → external state | Current file is observed, never auto-applied. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T23-01 | Information disclosure | compat/model | mitigate | raw credential only in internal explicit path; redacted DTO tests. |
| T23-02 | Spoofing | stable identity | mitigate | required identity parsing and deterministic key. |
| T23-03 | Tampering | unknown fields/shape | mitigate | raw object retention and explicit shape/version rejection. |
| T23-04 | DoS | malformed import | mitigate | per-item rejection and bounded parser input. |
| T23-05 | Elevation | plugin/host model boundary | mitigate | all business fields remain under Codex plugin. |
| T23-06 | Information disclosure | external auth file | mitigate | temp-only tests; no real home access. |
| T23-SC | Tampering | Cockpit reference | mitigate | synthetic fixtures only; no CC BY-NC-SA source copied. |
</threat_model>

<verification>
- `python3 -m pytest plugins/providers/builtin/codex/tests/test_account_compat.py plugins/providers/builtin/codex/tests/test_account_model.py -q`
- `git diff --check`
- Do not access real `~/.codex`, Cockpit datastore, or runtime state; do not build/package/install.
</verification>

<success_criteria>
- Four add paths share a precise Codex parser/model contract without auto-apply.
- Cockpit commit-pinned supported formats round-trip losslessly for unknown JSON fields.
</success_criteria>

<output>
Create `23-05-SUMMARY.md` after execution.
</output>
