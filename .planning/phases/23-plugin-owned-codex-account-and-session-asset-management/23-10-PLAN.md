---
phase: 23
plan: 10
title: 完成 Codex 账号前端与四源导入流程
type: execute
wave: 5
depends_on: [23-04, 23-07, 23-08]
requirements: [D-03, D-04, D-05, D-06, D-08, D-09, D-15, D-16, D-17, D-18, D-19, D-24, D-33, D-34, D-35, D-37]
files_modified:
  - plugins/providers/builtin/codex/plugin.yaml
  - plugins/providers/builtin/codex/frontend/account_entry.tsx
  - plugins/providers/builtin/codex/frontend/AccountOverview.tsx
  - plugins/providers/builtin/codex/frontend/AddAccountModal.tsx
  - mac-app/tests/accountFeatureCodex.test.mjs
autonomous: true
must_haves:
  truths:
    - "Codex plugin 页面自包含账号总览、responsive cards、当前/未应用/外部 matched/unmanaged、single/batch full export 与 explicit Apply。"
    - "Add modal 精确提供 OAuth、Token / JSON、API Key、导入四 tabs；import success 不暗示 Apply，secret 不在 list/diagnostic/log/frontend persistence。"
    - "UI 使用 OnlineWorker theme/ow-*、native focus/disabled/aria 状态，Cockpit 只提供 IA/flow 参考，不是 pixel/source copy。"
    - "Codex plugin TSX 由 23-04 的 generic Vite/TypeScript source root 实际纳入 typecheck，不依赖 plugin-local node_modules 或 host Codex import。"
    - "Codex `features.account` declaration 先校验 23-07 已存在的 backend entry，再与本计划创建的 frontend entry 同步落地，显式 `enabled: true`；不存在或 disabled 时 host 不显示入口。"
    - "本地导入与导出先请求 generic open/save capability，只把 opaque handle 交给 action；真实路径与完整 credential 不进入 React state。"
  artifacts:
    - path: plugins/providers/builtin/codex/plugin.yaml
      provides: "enabled generic account feature declaration pointing at existing entries"
    - path: plugins/providers/builtin/codex/frontend/account_entry.tsx
      provides: "build-time frontend entry consumed by generic host"
    - path: plugins/providers/builtin/codex/frontend/AddAccountModal.tsx
      provides: "four-tab account import modal"
    - path: mac-app/tests/accountFeatureCodex.test.mjs
      provides: "Codex plugin UI/state/source contracts"
  key_links:
    - "entry exports generic metadata/component contract while all labels/actions/models/errors remain under Codex frontend."
    - "23-04 的 generic build-time glob 与 TypeScript include 发现本 entry；mac-app 配置不列 Codex 路径。"
    - "plugin.yaml 只声明 generic feature metadata/entries；23-02 discovery 校验 enabled 与文件存在后才返回 descriptor。"
    - "actions call opaque host callback and display per-action state; no Provider runtime/owner bridge/Task Board/Usage/notification path."
---

<objective>
把已完成的账号 backend 接入 Codex plugin-owned frontend，完成四源导入、账号卡片、Apply/export 状态与可访问响应式 UI。

Purpose: 先交付账号闭环，之后才接 session asset UI；遵守 UI-SPEC 和 D-33–D-37。
Output: Codex build-time entry, overview, add modal, source/UI contract tests。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-CONTEXT.md
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-UI-SPEC.md:20-174, 242-285
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-PATTERNS.md:43-70
@mac-app/src/components/ActionGuideDialog.tsx:37-83, 143-159
@mac-app/src/index.css:182-322

<interfaces>
Entry receives neutral host callbacks/system capabilities and returns Codex-owned page component. Overview consumes redacted index DTO only; explicit actions are `import`, `apply`, `export`, and `refresh` through opaque callback. Local-file import and account export request open/save handles first and never receive paths. Modal preserves input on validation error and disables mutations while running.
</interfaces>
</context>

<tasks>
<task id="23-10-01" type="auto" tdd="true">
  <name>先锁定账号页面状态与四 tab UI contract</name>
  <files>mac-app/tests/accountFeatureCodex.test.mjs</files>
  <behavior>
    - Test 1: exact four tabs, empty/loading/import-invalid/import-success/apply-success/apply-failure/external states and secret-free card fields.
    - Test 2: single/batch export confirmation/save-handle/cancel, local import open-handle/cancel, replay/wrong-mode failures, selection disabled states, retry/diagnostic, and no path/credential in frontend state.
    - Test 3: native dialog semantics, keyboard tab/Escape/focus restore, aria-live/alert, responsive token usage, and no host/runtime imports.
    - Test 4: OAuth UI begins the generic loopback broker before start action, opens returned auth URL, awaits/cancels by opaque handle, forwards callback only transiently to complete action, and falls back to manual input when not listening/timeout.
    - Test 5: plugin manifest declares one enabled generic account feature only after the 23-07 backend and current-plan frontend entries exist; discovery descriptor maps exactly to the build-time entry.
  </behavior>
  <read_first>23-UI-SPEC.md:64-174, 242-285, 23-VALIDATION.md:49-89, mac-app/tests/commandRegistryView.test.mjs, mac-app/src/components/ActionGuideDialog.tsx:37-83</read_first>
  <action>[D-03, D-04, D-05, D-06, D-08, D-09, D-15, D-16, D-17, D-18, D-19, D-24, D-33, D-34, D-35, D-37] Add Node source/UI contracts before Codex components. Assert exact copy/tabs, import/apply separation, redacted list DTO, trusted open/save handle flow, full export confirmation, per-plugin errors, accessible controls, and excluded dashboard/live dependencies. Assert manifest metadata points to the existing 23-07 backend and the frontend created here, and generic discovery/build registries agree.
  <acceptance_criteria>Tests fail until account entry/overview/modal satisfy all UI-SPEC account states and prohibited controls are absent.</acceptance_criteria>
  <verify><automated>cd mac-app &amp;&amp; node --test tests/accountFeatureCodex.test.mjs</automated></verify>
  <done>Account UI state contract is executable and independent of installed app.</done>
</task>
<task id="23-10-02" type="auto">
  <name>实现 Codex account entry、overview 与 add modal</name>
  <files>plugins/providers/builtin/codex/plugin.yaml, plugins/providers/builtin/codex/frontend/account_entry.tsx, plugins/providers/builtin/codex/frontend/AccountOverview.tsx, plugins/providers/builtin/codex/frontend/AddAccountModal.tsx</files>
  <read_first>plugins/providers/builtin/codex/plugin.yaml, mac-app/vite.config.ts, mac-app/tsconfig.json, mac-app/src/components/ActionGuideDialog.tsx:37-83, 143-159, mac-app/src/index.css:182-322, 23-UI-SPEC.md:64-174</read_first>
  <action>[D-03, D-04, D-05, D-06, D-08, D-09, D-15, D-16, D-17, D-18, D-19, D-24, D-33, D-34, D-35, D-37] Verify the 23-07 backend entry exists, create the frontend entry, then add one generic `features.account` block with `enabled: true` to Codex plugin.yaml in the same change. Implement the self-contained account UI with redacted cards and scoped Apply/export. Implement four exact tabs. Local-file import requests an open handle; single/batch export requests a save handle after confirmation; cancellation is silent and no path/credential is retained. For OAuth, begin the generic loopback broker first, pass its redirect URI to start, open the URL, await/cancel by handle, and immediately forward captured URL to complete without logging/persisting it; retain manual callback fallback. Preserve accessibility/responsive states and keep business copy/actions in plugin files.</action>
  <acceptance_criteria>Root `tsc --noEmit` typechecks the external Codex plugin TSX and tests pass; entry is discoverable by the generic host glob; imported accounts remain non-current until Apply; cards never render full credentials; UI matches UI-SPEC responsive/keyboard/state matrix.</acceptance_criteria>
  <verify><automated>cd mac-app &amp;&amp; node --test tests/accountFeatureCodex.test.mjs tests/accountFeatureHost.test.mjs &amp;&amp; ./node_modules/.bin/tsc --noEmit &amp;&amp; git diff --check</automated></verify>
  <done>Codex account overview and four-source add flow are usable through generic host.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| redacted backend DTO → React | Account summary enters UI state. |
| secret input/native handle → explicit action | Credential input and opaque file selection cross only the scoped backend boundary. |
| plugin entry → generic host | Build-time component and metadata cross shared seam. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T23-01 | Information disclosure | React account UI | mitigate | redacted cards, no persistence/logs/secrets in list state. |
| T23-02 | Spoofing | OAuth/manual callback UI | mitigate | backend PKCE/state; UI exposes retry/error only. |
| T23-03 | Tampering | import/apply/export controls | mitigate | explicit confirmation and backend validation/rollback. |
| T23-04 | DoS | plugin loading/mutations | mitigate | per-plugin error boundary and native disabled controls. |
| T23-05 | Elevation | host/plugin UI | mitigate | Codex code stays in plugin; opaque host action. |
| T23-06 | Information disclosure | file/save dialogs | mitigate | one-use open/save handles; no native path or exported credential in React state. |
| T23-SC | Tampering | UI dependencies | mitigate | no new UI dependency or Cockpit source copy. |
</threat_model>

<verification>
- `cd mac-app && node --test tests/accountFeatureCodex.test.mjs tests/accountFeatureHost.test.mjs`
- `cd mac-app && ./node_modules/.bin/tsc --noEmit`
- `git diff --check`
- Do not run `pnpm build`, package, install, restart, or launch.
</verification>

<success_criteria>
- Account UI implements all four add paths and explicit Apply/export states without exposing secrets.
- Codex frontend is build-time plugin-owned and host remains generic.
</success_criteria>

<output>
Create `23-10-SUMMARY.md` after execution.
</output>
