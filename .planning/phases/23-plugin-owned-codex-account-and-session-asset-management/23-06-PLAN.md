---
phase: 23
plan: 6
title: 建立 AES-GCM 账号安全存储
type: execute
wave: 2
depends_on: [23-01, 23-05]
requirements: [D-13, D-14, D-19, D-22, D-23, D-24, D-25]
files_modified:
  - requirements.txt
  - plugins/providers/builtin/codex/python/account_store.py
  - plugins/providers/builtin/codex/tests/conftest.py
  - plugins/providers/builtin/codex/tests/test_account_store.py
autonomous: true
must_haves:
  truths:
    - "Plaintext index 只保存 redacted summary；每个 detail 使用 AES-256-GCM，random 32-byte key、12-byte nonce，key/detail/index 在 plugin-owned data dir。"
    - "key 文件与 credential detail/index 采用 0600、same-directory temp+fsync+replace、bounded backup；legacy plaintext 成功读取后才迁移加密。"
    - "tamper、错误 key/version、并发 mutation 或 replace failure 不泄漏 secret，且保持可恢复的旧字节/current marker。"
    - "plugin data root 下的 0600 cross-process operation lock 覆盖账号、OAuth、Apply 与 session mutation；23-07/08/09 复用同一锁。"
  artifacts:
    - path: plugins/providers/builtin/codex/python/account_store.py
      provides: "AESGCM key/index/detail/backup/legacy/concurrency storage"
    - path: plugins/providers/builtin/codex/tests/test_account_store.py
      provides: "crypto and atomicity regression coverage"
  key_links:
    - "本计划依赖 23-01 的 blocking-human checkpoint；未经确认不得修改 requirements.txt 或安装 cryptography。"
    - "store 仅接受 23-05 的 canonical record，并向 23-08/23-10 提供 secret-free list 与 explicit detail boundary。"
---

<objective>
在获批依赖后实现 Codex plugin-owned encrypted account store，保证凭据安全落盘、原子更新和可回滚迁移。

Purpose: 落实 D-22–D-25、T23-01/T23-04/T23-06，防止账号 detail 进入 shared storage、日志或 frontend persistence。
Output: requirements pin（仅在 checkpoint 后）、store、temp-only fixtures/guards。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-CONTEXT.md
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-RESEARCH.md:15-34, 171-217
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-VALIDATION.md:70-86
@plugins/providers/builtin/codex/python/account_model.py
@core/storage.py:121-161

<interfaces>
Store root is an explicit Codex plugin data directory, not Cockpit data. `index.json` is plaintext summary only; `key` is random 32 bytes; each detail is a versioned AESGCM envelope. `operation.lock` is a 0600 cross-process file lock/context manager reused by every later plugin mutation, not merely an in-memory worker lock. Public methods separate redacted list/detail/migration/current and lock/rollback behavior.
</interfaces>
</context>

<tasks>
<task id="23-06-01" type="auto" tdd="true">
  <name>先建立加密存储与真实目录拒绝测试</name>
  <files>plugins/providers/builtin/codex/tests/conftest.py, plugins/providers/builtin/codex/tests/test_account_store.py</files>
  <behavior>
    - Test 1: temp-only key/detail/index round-trip; key is 32 bytes/0600 and ciphertext excludes synthetic secrets.
    - Test 2: wrong key, tampered ciphertext/nonce/version, legacy plaintext migration failure, and simulated replace failure preserve source bytes.
    - Test 3: independent subprocess writers sharing one plugin root serialize on the same 0600 lock or fail cleanly; autouse guard rejects real home/Trash/Cockpit/OnlineWorker data.
  </behavior>
  <read_first>23-VALIDATION.md:70-86, 23-RESEARCH.md:171-217, tests/conftest.py:1-30, core/storage.py:121-161</read_first>
  <action>[D-13, D-14, D-19, D-22, D-23, D-24, D-25] Add tests and fixtures before implementation. Use synthetic secrets and explicit temp plugin root; test AESGCM, permissions, atomic backup, migration, redaction, and byte preservation. Spawn two independent processes to prove a 0600 file lock under that root serializes mutations; expose the same lock context for 23-07/08/09. Install no package before 23-01 approval.</action>
  <acceptance_criteria>Tests fail before store implementation and fail if any test opens the real home, Trash, Cockpit directory, or OnlineWorker data directory.</acceptance_criteria>
  <verify><automated>python3 -m pytest plugins/providers/builtin/codex/tests/test_account_store.py -q</automated></verify>
  <done>Crypto/storage safety contract and real-path deny guard are executable.</done>
</task>
<task id="23-06-02" type="auto">
  <name>实现 AES-256-GCM detail store 与原子迁移</name>
  <files>requirements.txt, plugins/providers/builtin/codex/python/account_store.py</files>
  <read_first>23-01-PLAN.md, plugins/providers/builtin/codex/python/account_model.py, 23-RESEARCH.md:15-34, 171-217</read_first>
  <action>[D-13, D-14, D-19, D-22, D-23, D-24, D-25] Only after 23-01 confirmation, pin `cryptography==48.0.1` and use AESGCM; do not implement crypto/call openssl. Generate random key/nonce, write versioned detail/index/key with 0600, same-directory temp+fsync+replace and bounded backup. Implement one cross-process `operation.lock` context under the plugin root and require all store mutations to hold it; later OAuth/apply/session plans import this same helper. Redact errors and migrate plaintext only after successful read.</action>
  <acceptance_criteria>All store tests pass; malformed/tampered records never overwrite source; key/detail/index are atomic and permission-tight; unknown model fields survive encrypted round-trip; no Cockpit path is used.</acceptance_criteria>
  <verify><automated>python3 -m pytest plugins/providers/builtin/codex/tests/test_account_store.py plugins/providers/builtin/codex/tests/test_account_model.py -q &amp;&amp; git diff --check</automated></verify>
  <done>Encrypted Codex account store is safe for explicit import/export/apply consumers.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| canonical account record → store | Credential detail enters encrypted plugin storage. |
| plugin data dir → filesystem | Atomic writes, permissions, backups, and concurrent mutation. |
| legacy plaintext → migration | Existing record is read and rewritten only after validation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T23-01 | Information disclosure | AESGCM detail/list/log | mitigate | AES-256-GCM, redacted index, no secret logs/frontend. |
| T23-02 | Spoofing | key/envelope identity | mitigate | versioned envelope and key_id validation. |
| T23-03 | Tampering | ciphertext/legacy migration | mitigate | InvalidTag/version rejection, source-preserving migration. |
| T23-04 | DoS/Tampering | atomic concurrent writes | mitigate | shared cross-process file lock, temp+fsync+replace, bounded backup/rollback. |
| T23-05 | Elevation | plugin storage ownership | mitigate | store root is Codex plugin data only; no host/runtime authority. |
| T23-06 | Information disclosure | filesystem scope | mitigate | explicit temp dir and deny-real-path fixture guard. |
| T23-SC | Tampering | cryptography package | mitigate | 23-01 blocking checkpoint + [VERIFIED] pin; no install before confirmation. |
</threat_model>

<verification>
- `python3 -m pytest plugins/providers/builtin/codex/tests/test_account_store.py plugins/providers/builtin/codex/tests/test_account_model.py -q`
- `git diff --check`
- No build/package/install/restart/launch; PyInstaller collection remains unverified until separately authorized.
</verification>

<success_criteria>
- Store meets exact AESGCM/key/nonce/permission/atomicity/rollback contract.
- Synthetic tests prove secrets and real user paths are excluded.
</success_criteria>

<output>
Create `23-06-SUMMARY.md` after execution.
</output>
