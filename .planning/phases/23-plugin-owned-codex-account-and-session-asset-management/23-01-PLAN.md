---
phase: 23
plan: 1
title: 通过依赖与前端根配置人工闸门
type: execute
wave: 1
depends_on: []
requirements: [D-03, D-37, D-22, D-23, D-24, D-25]
files_modified:
  - requirements.txt
  - mac-app/vite.config.ts
  - mac-app/tsconfig.json
autonomous: false
must_haves:
  truths:
    - "cryptography==48.0.1 只有在用户明确确认后才允许写入 requirements.txt 或安装。"
    - "确认范围明确包含新增 Python cryptography wheel、macOS universal2 arm64/x86_64 理由和删除 pin/恢复依赖的回滚面。"
    - "Vite/TypeScript 根配置只有在用户明确确认后才允许做最小 generic builtin-plugin frontend 路径扩展，且不得写入 Codex 特判。"
    - "未确认前没有依赖安装、构建、打包或运行时验证。"
  artifacts:
    - path: requirements.txt
      provides: "经确认后才可加入的 AESGCM 生产依赖"
    - path: mac-app/vite.config.ts
      provides: "经确认后才可加入的受限 builtin-plugin frontend build-time 读取范围"
    - path: mac-app/tsconfig.json
      provides: "经确认后才可加入的 generic plugin frontend typecheck 范围"
  key_links:
    - "23-06 的 account_store 实现只能依赖本计划的 blocking-human checkpoint。"
    - "23-04 的外部 builtin frontend 编译配置只能依赖本计划的 blocking-human checkpoint。"
    - "RESEARCH.md Package Legitimacy Audit 的 [VERIFIED] cryptography==48.0.1 与本 checkpoint 保持一致。"
---

<objective>
在任何依赖或根配置修改前锁定 AES-256-GCM 依赖与 generic plugin frontend 编译闸门，避免把计划结论误当成已获授权的配置变更。

Purpose: D-22–D-25 需要新的 AESGCM 生产依赖，D-03/D-37 需要让 Vite/TypeScript 读取仓内插件前端；两者都涉及根配置，必须先取得确认。
Output: 一个只含 blocking human checkpoint 的配置计划；通过后才可执行 23-04/23-06。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-CONTEXT.md
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-RESEARCH.md
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-VALIDATION.md
</context>

<tasks>
<task id="23-01-01" type="checkpoint:human-verify" gate="blocking-human">
  <name>确认 cryptography 依赖、前端根配置与回滚边界</name>
  <files>requirements.txt, mac-app/vite.config.ts, mac-app/tsconfig.json</files>
  <read_first>23-CONTEXT.md, 23-RESEARCH.md:15-34, 75-120, 23-VALIDATION.md, requirements.txt, mac-app/vite.config.ts, mac-app/tsconfig.json</read_first>
  <action>[D-03, D-37, D-22, D-23, D-24, D-25] 在修改前暂停并请求一次精确确认：(1) 在 `requirements.txt` 新增 `cryptography==48.0.1`，理由是提供 AESGCM 且保留 macOS universal2 arm64/x86_64 wheel；(2) 对 `mac-app/vite.config.ts` 与 `mac-app/tsconfig.json` 做最小 provider-neutral 扩展，只允许仓内 `plugins/providers/builtin/*/frontend` 进入 build-time glob、React 去重与 typecheck，不写 Codex 路径或业务字段。回滚面是删除该 pin，并逐字恢复两份前端配置。确认前不修改、不安装、不 build/package/install/restart；确认也不授权后四项运行。</action>
  <verify><manual>用户明确确认或拒绝上述精确依赖变更；未确认时计划保持阻塞。</manual></verify>
  <done>依赖与根配置授权状态可审计；23-04/23-06 只有在该 checkpoint 通过后才可执行。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan executor → requirements.txt/PyPI | 依赖是供应链边界。 |
| Plan executor → Vite/TypeScript config | frontend source roots 与 module resolution 是构建边界。 |
| Codex plugin → encrypted store | 后续 plaintext index、key、ciphertext detail 的权限边界。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T23-01 | Information disclosure | credential dependency/store | mitigate | 仅批准 AESGCM 依赖，后续 secret-free DTO 与加密 detail。 |
| T23-02 | Spoofing/Tampering | OAuth | mitigate | 后续 plan 强制 PKCE/state；本计划不运行 OAuth。 |
| T23-03 | Tampering | imported JSON/ZIP | mitigate | 后续 plan 强制结构、hash、path、size 校验。 |
| T23-04 | Tampering/DoS | atomic writes | mitigate | 后续 plan 强制 temp+fsync+replace+backup/rollback。 |
| T23-05 | Elevation/Information disclosure | host/plugin boundary | mitigate | checkpoint 不引入 provider-specific host code。 |
| T23-06 | Information disclosure | real home/data dirs | mitigate | 不执行安装、运行或真实目录访问。 |
| T23-SC | Tampering | Python package supply chain | mitigate | RESEARCH Package Legitimacy Audit 已 [VERIFIED]；blocking human checkpoint 先于 requirements 修改。 |
</threat_model>

<verification>
- 仅确认 checkpoint；不得自动安装 `cryptography`。
- `git diff --check` 在计划文档变更后运行。
</verification>

<success_criteria>
- 用户确认记录明确写出依赖、受限 plugin frontend source root、平台理由和三份文件的回滚面。
- 未确认时 requirements.txt、Vite/TypeScript 配置与环境保持不变。
</success_criteria>

<output>
执行完成后创建 `23-01-SUMMARY.md`，记录 checkpoint 状态；拒绝或未确认则保留 blocking 状态。
</output>
