---
phase: 23
plan: 9
title: 实现离线 Codex 会话资产后端
type: execute
wave: 5
depends_on: [23-08]
requirements: [D-26, D-27, D-28, D-29, D-30, D-31, D-32, D-40]
files_modified:
  - plugins/providers/builtin/codex/python/account_feature.py
  - plugins/providers/builtin/codex/python/session_assets.py
  - plugins/providers/builtin/codex/python/session_package.py
  - plugins/providers/builtin/codex/tests/test_session_assets.py
autonomous: true
must_haves:
  truths:
    - "Session list/search/expand 与近 30 天 token/cost 直接读取一次解析的 effective Codex Home，本地成本不可用时显示 unavailable 而非伪造 0。"
    - "ZIP manifest kind/version/file size/SHA-256/path/symlink/conflict 语义按 Cockpit commit 35963163813d7424b63cd6053874ce5fc7973d03；同 id 同 hash skip、不同 hash conflict，绝不静默覆盖。"
    - "Session action 在 backend 内一次解析并 canonicalize effective home；payload home/path/session_root 无效，ZIP 文件只通过同 feature 的 trusted open/save handle 进入。"
    - "ZIP import/export、trash/restore、visibility repair 复用 23-06 同一个 cross-process operation lock；失败按文件/index/SQLite 边界完整 rollback。"
    - "Visibility repair 精确实现 Cockpit current quick：只改 root/sqlite 下两个 official state_5.sqlite 及其引用 rollout，不改 session_index/codex-dev/timestamps/schema。"
    - "Trash/restore manifest-backed 可逆；不永久删除、不跨 home、不发 EventBus/session/notification。"
    - "Codex plugin handler 在本计划追加 session list/export/import/trash/restore/repair actions；host/main 不出现 session 业务 command。"
  artifacts:
    - path: plugins/providers/builtin/codex/python/session_assets.py
      provides: "effective-home parser/list/search/30d usage/trash/restore/repair"
    - path: plugins/providers/builtin/codex/python/session_package.py
      provides: "Cockpit-compatible ZIP manifest/import/export/integrity/conflict"
    - path: plugins/providers/builtin/codex/tests/test_session_assets.py
      provides: "offline asset mutation and real-path guard tests"
  key_links:
    - "所有 parser/scan 函数显式接收同一个 effective home；不复用硬编码 ~/.codex 的 storage_runtime 常量。"
    - "account_feature.py 在持有 account_store.operation_lock 时路由 ZIP/trash/restore/repair，模块不创建第二把锁。"
    - "session_package 只兑换 trusted native handle；真实导入/导出路径不进入 payload、React state 或响应。"
    - "visibility repair 的 provider 来自 effective-home config.toml（缺省 openai），只触碰 official state DB 既有 threads 行及其引用 rollout。"
---

<objective>
在 account action 之外实现完全离线、当前 effective Codex Home 限定的会话资产后端，覆盖浏览、30 天统计、ZIP、冲突、废纸篓和 visibility repair。

Purpose: 满足 D-26–D-32，同时保持它不是 OnlineWorker live Sessions/Usage 的扩展。
Output: Codex-owned session modules and synthetic file/archive tests。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-CONTEXT.md
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-RESEARCH.md:171-250
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-PATTERNS.md:71-96
@.planning/phases/23-plugin-owned-codex-account-and-session-asset-management/23-VALIDATION.md:70-89
@plugins/providers/builtin/codex/python/storage_runtime.py:15-55, 129-225, 271-312
@plugins/providers/builtin/codex/python/account_store.py

<interfaces>
The Codex handler rejects payload `home`/`path`/`session_root`/`CODEX_HOME`, resolves/canonicalizes effective home once with the 23-08 resolver, and passes it explicitly; modules never resolve literal `~/.codex`. ZIP import/export redeem only trusted `open`/`save` handles. `session_package.py` emits `kind=codex-session-export`, `package_version=1`, `exported_at`, `sessions[]` with exact manifest item fields and safe `files/...` entries. Results are per-item imported/skipped/rejected/conflict. Current quick visibility repair reads `config.toml.model_provider` (default `openai`), inspects only `<home>/state_5.sqlite` and `<home>/sqlite/state_5.sqlite`, updates existing `threads.model_provider` rows and their referenced rollout first `session_meta`, and never changes schema, timestamps, `session_index.jsonl`, or `sqlite/codex-dev.db`.
</interfaces>
</context>

<tasks>
<task id="23-09-01" type="auto" tdd="true">
  <name>先建立本地会话与 ZIP/trash/repair fixtures</name>
  <files>plugins/providers/builtin/codex/tests/test_session_assets.py</files>
  <behavior>
    - Test 1: temp effective home list/title search/expand, 30-day boundary, duplicate snapshot/fork handling, and unavailable cost state.
    - Test 2: exact manifest/version/hash/size/path validation; valid import, same-id same-hash skip, different-hash conflict, malformed/oversized/traversal/symlink rejection and index rollback.
    - Test 3: manifest-backed trash/restore and current quick visibility repair success/no-op/failure rollback without permanent delete or live events.
    - Test 4: visibility fixtures cover both official state DB paths, target provider config/default, existing threads schema/transaction and referenced rollout first-line update; codex-dev/session index/timestamps/schema stay byte-for-byte unchanged.
    - Test 5: payload home/path/session_root and effective-home symlink/escape attempts fail before IO; real home/Trash/data stay untouched.
    - Test 6: ZIP import/export require same-feature open/save handles; cancel/replay/wrong-mode/cross-feature/symlink handles write nothing and responses contain no path.
    - Test 7: independent session mutations serialize through the 23-06 operation lock; Codex handler routes only scoped session actions and unknown actions fail closed.
  </behavior>
  <read_first>23-CONTEXT.md: D-26-D-32, 23-RESEARCH.md:171-250, 23-VALIDATION.md:87-89, plugins/providers/builtin/codex/python/storage_runtime.py:15-55, 129-225, 271-312</read_first>
  <action>[D-26, D-27, D-28, D-29, D-30, D-31, D-32, D-40] Build synthetic sessions/index/rollout/ZIP/trash fixtures under temp dirs. Add both official state DB fixtures with pre-existing `threads` tables and an unrelated `sqlite/codex-dev.db`; pin current quick provider/row/referenced-rollout behavior and prove unrelated files/schema/timestamps stay unchanged. Pin the Cockpit ZIP fields/conflicts. Inject trusted handles and concurrent subprocess mutations; add payload/home/symlink/real-path denies and live-surface spies.</action>
  <acceptance_criteria>Tests fail until all local parser/package/mutation semantics are implemented and cannot pass by reading existing OnlineWorker projections.</acceptance_criteria>
  <verify><automated>python3 -m pytest plugins/providers/builtin/codex/tests/test_session_assets.py -q</automated></verify>
  <done>Offline session asset behavior is test-first locked with synthetic fixtures.</done>
</task>
<task id="23-09-02" type="auto">
  <name>实现 effective-home 会话资产与可逆文件事务</name>
  <files>plugins/providers/builtin/codex/python/account_feature.py, plugins/providers/builtin/codex/python/session_assets.py, plugins/providers/builtin/codex/python/session_package.py</files>
  <read_first>plugins/providers/builtin/codex/python/account_feature.py, plugins/providers/builtin/codex/python/account_store.py, plugins/providers/builtin/codex/python/apply.py, plugins/providers/builtin/codex/python/storage_runtime.py:15-55, 129-225, 271-312, 23-RESEARCH.md:171-250</read_first>
  <action>[D-26, D-27, D-28, D-29, D-30, D-31, D-32, D-40] Reject payload paths, resolve/canonicalize effective home once with the 23-08 helper, and pass it to parameterized parsing over `sessions/`, `archived_sessions/`, and `session_index.jsonl`; implement title-only search, expandable metadata, 30-day snapshot dedupe/fork handling, and unavailable cost. Redeem trusted open/save handles for exact Cockpit v1 ZIP import/export with bounded size/count/path/hash checks, per-item conflicts and rollback. Under the single 23-06 operation lock, implement manifest-backed trash/restore and current quick visibility repair: read target provider from config/default, transactionally update only existing rows in root/sqlite official `state_5.sqlite`, and atomically repair their referenced rollout first `session_meta`; do not mutate index/codex-dev/timestamps/schema. Wire only session list/export/import/trash/restore/repair actions and return redacted per-item results. Never use live projections/events, process inspection, multiple homes, sync, or permanent delete.</action>
  <acceptance_criteria>All session tests pass through the Codex plugin handler; valid packages never overwrite conflicts; failures restore files/index; trash is recoverable; repair reports counts; host/main remain unchanged and generic.</acceptance_criteria>
  <verify><automated>python3 -m pytest plugins/providers/builtin/codex/tests/test_session_assets.py -q &amp;&amp; git diff --check</automated></verify>
  <done>Codex plugin owns offline session asset operations independent of OnlineWorker live surfaces.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| ZIP/manifest/path → session package | Imported archive controls paths, hashes, sizes, and metadata. |
| trusted handle/effective home → local mutation | Native file and current-home assets are resolved outside payload, serialized, and modified with rollback. |
| session results → UI | Titles/metadata/cost status cross frontend without content replay. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T23-01 | Information disclosure | session details/cost | mitigate | list only bounded metadata; no transcript/event replay. |
| T23-02 | Spoofing | session id/manifest | mitigate | identity/path/index consistency checks. |
| T23-03 | Tampering | ZIP/files/index | mitigate | size/hash/path/version validation and no overwrite conflicts. |
| T23-04 | DoS/Data loss | archive/repair/trash | mitigate | one cross-process lock, bounded package, SQLite transaction, backup, temp replace, rollback. |
| T23-05 | Elevation | live surface boundary | mitigate | no EventBus/session/Usage/notification/app-server/process calls. |
| T23-06 | Information disclosure | native path/home/Trash scope | mitigate | trusted one-use handles, backend-only home resolution, symlink containment, temp-only deny guards. |
| T23-SC | Tampering | Cockpit format | mitigate | synthetic commit-pinned fixtures; independent implementation. |
</threat_model>

<verification>
- `python3 -m pytest plugins/providers/builtin/codex/tests/test_session_assets.py -q`
- `git diff --check`
- Do not run build/package/install/launch or real home/Trash operations.
</verification>

<success_criteria>
- Session backend matches pinned ZIP/trash semantics and Cockpit current quick visibility repair, with shared serialization and rollback.
- No OnlineWorker live projection or multiple-home behavior is involved.
</success_criteria>

<output>
Create `23-09-SUMMARY.md` after execution.
</output>
