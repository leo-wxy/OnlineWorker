# Phase 20 Plan 02 — Maintenance UI And Installed Verification

## Goal

Expose diagnostics and privacy-safe export in the existing Maintenance panel and validate the installed app flow.

## Tasks

1. Add typed frontend contracts and state/actions to `MaintenanceSettingsPanel.tsx` with overlap guards.
2. Render compact grouped results, remediation/detail expansion, copy summary, export, and Finder reveal states per `20-UI-SPEC.md`.
3. Add Chinese and English strings that explicitly state local-only export and exclusion of credentials and Session content.
4. Add frontend contract tests covering command wiring, states, privacy copy, and no new top-level Tab.
5. Run frontend build and full focused regression.
6. After explicit packaging permission, build/install and manually verify healthy/partial diagnostics, save cancellation, ZIP contents, and Finder reveal.

## Verification

- frontend Node tests
- `npm run build` in `mac-app`
- focused/full Rust tests
- `git diff --check`
- installed-app UAT remains a separate explicit-permission gate
