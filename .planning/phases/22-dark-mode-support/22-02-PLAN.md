---
phase: 22
plan: 2
title: 建立语义 token 与正式主题规范
type: execute
wave: 1
depends_on: []
requirements: [D-05, D-06, D-07, D-12, D-13, D-14]
files_modified:
  - mac-app/src/index.css
  - mac-app/tests/themeContract.test.mjs
  - docs/UI-THEME.md
  - docs/README.md
  - CONTRIBUTING.md
autonomous: true
must_haves:
  truths:
    - "Light 与 Dark 共享一套语义层级、圆角、边框、状态和阴影；暗色为深蓝灰玻璃，浅色为冷白/雾蓝对应版本。"
    - "index.css 是 token 真相源，既有 --ow-* alias 与组件类继续可用；新 UI 只消费语义 token 或既有 ow-* 类。"
    - "docs/UI-THEME.md、两套 CSS token 和可运行 contract test 保持一致；贡献入口不复制 token 内容。"
  artifacts:
    - mac-app/src/index.css
    - docs/UI-THEME.md
    - mac-app/tests/themeContract.test.mjs
  key_links:
    - "themeContract.test.mjs 同时读取 docs/UI-THEME.md 与 index.css，核对 token 在 light/dark 块均存在。"
    - "docs/README.md 索引规范，CONTRIBUTING.md 只提供链接。"
---

# 22-02：建立语义 token 与正式主题规范

## Objective

扩展现有 `--ow-*` 和 `.ow-*` 为一套可供当前与后续 UI 直接复用的双主题契约，并将规则固化为唯一稳定开发文档。

## Tasks

<task id="22-02-01" type="auto">
  <name>先建立 CSS 与文档契约测试</name>
  <files>mac-app/tests/themeContract.test.mjs</files>
  <read_first>22-CONTEXT.md, 22-RESEARCH.md, 22-PATTERNS.md, mac-app/src/index.css, mac-app/tests/appShell.test.mjs, docs/README.md</read_first>
  <action>[D-07, D-12, D-13, D-14] 新建 Node contract test，读取 CSS 与主题文档，断言 `data-ow-theme` light/dark 块、`--ow-bg`、surface/input/code/text/line/focus/status/overlay/shadow、现有 alias、约 150ms 颜色属性过渡、reduced-motion 规则、文档索引和硬编码禁令。允许例外必须按语义精确列出，不允许整目录忽略。</action>
  <acceptance_criteria>测试会在缺一套 token、文档漂移、删除兼容 alias、使用整窗 opacity/transition-all 或缺少 reduced-motion 时失败。</acceptance_criteria>
  <verify><automated>cd mac-app &amp;&amp; node --test tests/themeContract.test.mjs</automated></verify>
  <done>双主题、兼容 alias、动效与文档契约已由失败测试锁定。</done>
</task>

<task id="22-02-02" type="auto">
  <name>实现双主题 token 与复用表面</name>
  <files>mac-app/src/index.css</files>
  <read_first>mac-app/src/index.css, docs/screenshots/dashboard.png, 22-RESEARCH.md:100-131</read_first>
  <action>[D-05, D-06, D-07, D-12, D-13] 在 `:root[data-ow-theme="light"]`/`dark` 定义最小语义 token，补齐 `--ow-bg`；保留旧 token alias，并让现有 shell/page/toolbar/segment/button/modal/log/chat/scrollbar 类消费 token。Dark 使用和谐深蓝灰玻璃、细冷灰线和柔和近白文字；Light 使用冷白/雾蓝对应材质。过渡只覆盖 color/background-color/border-color/box-shadow/fill/stroke，约 150ms，并在 reduced-motion 下关闭或缩短。不得修改布局、tailwind.config.js 或增加单场景 token。</action>
  <acceptance_criteria>两主题拥有相同语义角色；既有组件类不需要主题分支；当前未定义的 --ow-bg 已补齐；没有新依赖或完整组件库。</acceptance_criteria>
  <verify><automated>cd mac-app &amp;&amp; node --test tests/themeContract.test.mjs &amp;&amp; ./node_modules/.bin/tsc --noEmit</automated></verify>
  <done>Light/Dark token、现有 ow-* 表面与 reduced-motion 规则通过契约和类型检查。</done>
</task>

<task id="22-02-03" type="auto">
  <name>发布单一 UI 主题开发规范</name>
  <files>docs/UI-THEME.md, docs/README.md, CONTRIBUTING.md</files>
  <read_first>22-CONTEXT.md, 22-RESEARCH.md:234-252, docs/README.md, CONTRIBUTING.md</read_first>
  <action>[D-13, D-14] 编写 `docs/UI-THEME.md`，覆盖 preference/resolved model、首帧与双窗口边界、token 表、组件消费、禁止硬编码、状态/焦点/对比度、透明 menubar、增量扩展、reduced-motion 和验证命令。`docs/README.md` 增加唯一索引；`CONTRIBUTING.md` 只加入口链接，不复制 token 表；产品 README 不改。</action>
  <acceptance_criteria>后续 UI 开发者能仅凭该文档选择正确 token、添加新 token 并运行契约检查；CSS 与文档 token 全部由测试核对。</acceptance_criteria>
  <verify><automated>cd mac-app &amp;&amp; node --test tests/themeContract.test.mjs</automated></verify>
  <done>唯一稳定主题规范已建立并从现有文档入口可发现。</done>
</task>

## Verification

- `cd mac-app && node --test tests/themeContract.test.mjs`
- `cd mac-app && ./node_modules/.bin/tsc --noEmit`
- `git diff --check`

## Must Not

- 不新增 ThemeProvider、主题库、Tailwind dark 配置或组件库。
- 不在 README/CONTRIBUTING/Phase 文件重复维护 token 真相。
