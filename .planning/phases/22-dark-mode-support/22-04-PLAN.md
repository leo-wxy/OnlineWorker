---
phase: 22
plan: 4
title: 迁移 Dashboard 与 Task Board 主题表面
type: execute
wave: 3
depends_on: [22-03]
requirements: [D-05, D-06, D-07, D-09, D-13]
files_modified:
  - mac-app/src/pages/TaskBoard.tsx
  - mac-app/src/components/dashboard/DashboardAlerts.tsx
  - mac-app/src/components/dashboard/DashboardError.tsx
  - mac-app/src/components/dashboard/DashboardHero.tsx
  - mac-app/src/components/dashboard/DashboardSidebar.tsx
  - mac-app/src/components/dashboard/ProviderStatusList.tsx
  - mac-app/src/components/dashboard/SettingSwitch.tsx
  - mac-app/src/components/dashboard/model.ts
  - mac-app/tests/taskBoard.test.mjs
  - mac-app/tests/dashboardProviderStatus.test.mjs
autonomous: true
must_haves:
  truths:
    - "Dashboard 和 Task Board 的正常、状态、交互与审批表面在 Light/Dark 下均消费语义 token。"
    - "provider/status/lane 的选择逻辑保持原样，只替换颜色来源；业务状态机、布局与动作顺序不变。"
  artifacts:
    - mac-app/src/pages/TaskBoard.tsx
    - mac-app/src/components/dashboard/model.ts
  key_links:
    - "dashboard/model.ts 与 TaskBoard tone map 返回 token/语义类，并由现有组件消费。"
---

# 22-04：迁移 Dashboard 与 Task Board 主题表面

## Objective

在不动卡片尺寸、grid、provider 状态或 Task Board 控制逻辑的前提下，完成总览与任务区全部颜色/材质/状态迁移。

## Tasks

<task id="22-04-01" type="auto">
  <name>锁定 Dashboard 与 Task Board 结构/状态契约</name>
  <files>mac-app/tests/taskBoard.test.mjs, mac-app/tests/dashboardProviderStatus.test.mjs</files>
  <read_first>22-RESEARCH.md:133-171, mac-app/tests/taskBoard.test.mjs, mac-app/tests/dashboardProviderStatus.test.mjs</read_first>
  <action>[D-06, D-09] 扩展现有 source tests，保留 Dashboard 服务动作、provider 状态和 Task Board lane/selection/approval/interrupt/recover 结构；增加语义 token 消费与无新增固定浅色 surface 的窄断言。</action>
  <acceptance_criteria>测试能识别布局/行为入口被删除、provider 状态映射丢失或重新引入直接浅色表面。</acceptance_criteria>
  <verify><automated>cd mac-app &amp;&amp; node --test tests/taskBoard.test.mjs tests/dashboardProviderStatus.test.mjs</automated></verify>
  <done>Dashboard 与 Task Board 的结构和状态行为由测试锁定。</done>
</task>

<task id="22-04-02" type="auto">
  <name>迁移 Dashboard 状态与操作表面</name>
  <files>mac-app/src/components/dashboard/DashboardAlerts.tsx, mac-app/src/components/dashboard/DashboardError.tsx, mac-app/src/components/dashboard/DashboardHero.tsx, mac-app/src/components/dashboard/DashboardSidebar.tsx, mac-app/src/components/dashboard/ProviderStatusList.tsx, mac-app/src/components/dashboard/SettingSwitch.tsx, mac-app/src/components/dashboard/model.ts</files>
  <read_first>mac-app/src/components/dashboard/model.ts, mac-app/src/components/dashboard/ProviderStatusList.tsx, mac-app/src/index.css</read_first>
  <action>[D-05, D-06, D-07, D-09, D-13] 将 hero、alerts、sidebar、quick actions、provider health、service buttons、switch 的 surface/text/line/focus/disabled/loading/warning/error 替换为全局 token/`.ow-*`；状态映射仍由 model 集中管理。只改 class/颜色来源，不改 props、状态计算、DOM 顺序或尺寸。</action>
  <acceptance_criteria>Dashboard 关键状态在两主题可读；provider healthy/degraded/stopped/unknown 语义保持；无页面专属主题分支。</acceptance_criteria>
  <verify><automated>cd mac-app &amp;&amp; node --test tests/dashboardProviderStatus.test.mjs &amp;&amp; ./node_modules/.bin/tsc --noEmit</automated></verify>
  <done>Dashboard 状态与操作表面完成 token 迁移并通过验证。</done>
</task>

<task id="22-04-03" type="auto">
  <name>迁移 Task Board lanes、卡片与控制状态</name>
  <files>mac-app/src/pages/TaskBoard.tsx</files>
  <read_first>mac-app/src/pages/TaskBoard.tsx:198-252,300-430,962-1210, mac-app/src/index.css</read_first>
  <action>[D-05, D-06, D-07, D-09, D-13] 把 provider/lane tone、cards、selected/pinned、focus、approval allow/deny、interrupt/recover/continue、详情、loading/error/empty/batch actions 迁移到语义 token。保留 hash/provider 选择、lane 分类、移动布局和所有 command 调用。</action>
  <acceptance_criteria>所有 Task Board 状态在 Light/Dark 有可读前景/背景/focus；没有变更状态机、数据流、卡片结构或操作顺序。</acceptance_criteria>
  <verify><automated>cd mac-app &amp;&amp; node --test tests/taskBoard.test.mjs &amp;&amp; ./node_modules/.bin/tsc --noEmit</automated></verify>
  <done>Task Board 全状态主题化且原业务契约保持。</done>
</task>

## Verification

- `cd mac-app && node --test tests/taskBoard.test.mjs tests/dashboardProviderStatus.test.mjs`
- `cd mac-app && ./node_modules/.bin/tsc --noEmit`
- 人工检查 normal/hover/focus/selected/disabled/loading/empty/warning/error；对照现有 Dashboard 布局。
