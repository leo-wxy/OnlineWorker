---
phase: 22
plan: 3
title: 统一 App shell 与 menubar 主题体验
type: execute
wave: 2
depends_on: [22-01, 22-02]
requirements: [D-01, D-03, D-04, D-06, D-08, D-09, D-10, D-12, D-13]
files_modified:
  - mac-app/src/App.tsx
  - mac-app/src/components/menubar-popover/MenubarPopover.tsx
  - mac-app/src/utils/menubarPopover.ts
  - mac-app/tests/appShell.test.mjs
  - mac-app/tests/menubarPopover.test.mjs
autonomous: true
must_haves:
  truths:
    - "System/Light/Dark 选择器位于展开侧栏的语言控件附近，侧栏收起时隐藏，不新增页面或 menubar 设置入口。"
    - "App shell 与 menubar 使用同一 token 和 shared sync；menubar 的透明外壳、现有内容、尺寸与操作保持不变。"
    - "迁移仅替换颜色、材质、focus 与状态表达，不改变导航、布局、数据调用或信息密度。"
  artifacts:
    - mac-app/src/App.tsx
    - mac-app/src/components/menubar-popover/MenubarPopover.tsx
    - mac-app/tests/appShell.test.mjs
    - mac-app/tests/menubarPopover.test.mjs
  key_links:
    - "App 只调用 theme.ts 读写偏好；MenubarPopover 不拥有偏好 state。"
    - "menubarPopover.ts 的 provider/status 映射只返回语义 token/class。"
---

# 22-03：统一 App shell 与 menubar 主题体验

## Objective

把主题入口与两个最高层 UI 表面接到共享运行时/token，同时严格保留当前主窗口布局和 menubar 紧凑信息结构。

## Tasks

<task id="22-03-01" type="auto">
  <name>扩展 App shell 与 menubar 结构契约</name>
  <files>mac-app/tests/appShell.test.mjs, mac-app/tests/menubarPopover.test.mjs</files>
  <read_first>22-CONTEXT.md, 22-VALIDATION.md, mac-app/tests/appShell.test.mjs, mac-app/tests/menubarPopover.test.mjs</read_first>
  <action>[D-03, D-04, D-06, D-08, D-10] 添加失败断言：主题选择器紧邻语言 segment 且只在展开分支；App 调用 shared theme setter；menubar 无第二 writer/selector；Overview、Provider、Usage、Tasks/Sessions/Usage 入口和透明 root 仍存在；两处不再新增直接浅色 surface 类。</action>
  <acceptance_criteria>测试能阻止选择器位置漂移、侧栏收起仍显示、menubar 内容重做或独立主题状态。</acceptance_criteria>
  <verify><automated>cd mac-app &amp;&amp; node --test tests/appShell.test.mjs tests/menubarPopover.test.mjs tests/theme.test.mjs</automated></verify>
  <done>App shell 与 menubar 结构/主题边界由测试锁定。</done>
</task>

<task id="22-03-02" type="auto">
  <name>接入侧栏主题选择器并迁移 App shell</name>
  <files>mac-app/src/App.tsx</files>
  <read_first>mac-app/src/App.tsx:241-399, mac-app/src/utils/theme.ts, mac-app/src/index.css</read_first>
  <action>[D-01, D-03, D-04, D-06, D-09, D-12, D-13] 在展开侧栏的语言控件附近增加可访问的三档 segment，复用现有尺寸与语义类，收起分支不渲染。把品牌卡、sidebar tab、attention badge、service 状态、first-run banner 和 loading shell 的直接浅色类替换为 token/`.ow-*`；只读写 ThemePreference，不把 ResolvedTheme 下发给页面。</action>
  <acceptance_criteria>三档选择立即生效；侧栏展开/收起行为、宽度、tab、banner 和路由不变；App 中主题表面不再依赖 bg-white/text-gray/text-slate 等固定浅色类。</acceptance_criteria>
  <verify><automated>cd mac-app &amp;&amp; node --test tests/appShell.test.mjs tests/theme.test.mjs &amp;&amp; ./node_modules/.bin/tsc --noEmit</automated></verify>
  <done>主题选择器与 App shell 迁移通过定向测试和类型检查。</done>
</task>

<task id="22-03-03" type="auto">
  <name>让 menubar 完整消费共享主题</name>
  <files>mac-app/src/components/menubar-popover/MenubarPopover.tsx, mac-app/src/utils/menubarPopover.ts</files>
  <read_first>mac-app/src/components/menubar-popover/MenubarPopover.tsx:58-85,261-736, mac-app/src/utils/menubarPopover.ts:38-125, docs/superpowers/specs/2026-07-07-menubar-popover-design.md</read_first>
  <action>[D-05, D-06, D-08, D-09, D-10, D-13] 保留 html/body/root 透明处理与现有 DOM/交互，只将 inner panel、header、nav、main、footer、provider rows、usage、session states、hover/focus/selected/loading/empty 迁移到语义 token；provider/status helper 保留选择逻辑但不再返回固定浅色值。不得增加主题入口、卡片、指标或操作。</action>
  <acceptance_criteria>menubar 暗色不出现白卡片/纯黑块，浅色保持当前布局；现有 tabs、导航、refresh、rows 和底部入口均保留，外层仍透明。</acceptance_criteria>
  <verify><automated>cd mac-app &amp;&amp; node --test tests/menubarPopover.test.mjs tests/theme.test.mjs &amp;&amp; ./node_modules/.bin/tsc --noEmit</automated></verify>
  <done>menubar 全部现有内容消费共享主题且透明外壳不变。</done>
</task>

## Verification

- `cd mac-app && node --test tests/appShell.test.mjs tests/menubarPopover.test.mjs tests/theme.test.mjs`
- `cd mac-app && ./node_modules/.bin/tsc --noEmit`
- 手动 source-mode 检查侧栏展开/收起与 menubar 透明边缘；不声称安装态通过。
