---
phase: 24
plan: 09
title: 精简重复基础逻辑与失效入口
type: refactor
wave: 8
depends_on: [24-08]
requirements: [STAB-02, STAB-05, STAB-06]
status: completed
completed_at: "2026-09-05T10:33:45+08:00"
---

# 24-09 Plan: 行为保持的代码精简

## 范围

1. 删除前端无调用的常量、格式化函数、创建会话包装函数与相同结果分支。
2. bot wrapper 能力查询复用 core registry，保留调用名称和插件发现边界。
3. 共享 Rust CLI 首 token、HOME 展开、PATH 构造与 sidecar 环境，保留入口差异。
4. 收敛六个设置 command 的读取、normalize、修改、序列化和原子保存外壳。
5. 收敛会话模块六个 owner bridge 单次请求的传输分帧，保留各业务入口的连接失败、超时、响应解析与 fallback 决策；其他模块的差异化错误处理不在本次合并。

## 成功标准与验证

- 净减少生产代码；不新增依赖或公开合同。
- wrapper 自定义 provider 的命令与回调回归通过。
- 前端 Task Board、新会话、Menubar 测试及 TypeScript 检查通过。
- Rust CLI/环境、owner bridge 响应与失败语义、配置恢复、service 生命周期回归通过。
- Rust 格式检查及 git diff 空白检查通过。

## 保留边界

- 24-07 在本切片实施时保持 blocked；Phase 24 归档时已由用户批准延期，不声称跨 owner interrupt 已接通。
- 保留旧 TUI helper、source claim、EventBus 去重、LKG、generation/handle、原子写入与备份恢复。
- 不修改运行时用户配置，不扩展 Settings 并发保存或跨进程 writer 设计。
- 源码精简完成后，按后续用户明确授权执行快速打包、覆盖安装与重启验证；结果见 24-09-SUMMARY.md。功能 UAT、完整发布验证和 push 不在本次交付范围。
