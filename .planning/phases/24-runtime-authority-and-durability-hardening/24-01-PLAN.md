---
phase: 24
plan: 01
title: 限定 MessageEventBus 去重保留窗口
type: execute
wave: 1
depends_on: []
requirements: [STAB-01]
files_modified:
  - core/messages/bus.py
  - tests/test_message_event_bus.py
autonomous: true
must_haves:
  truths:
    - "MessageEventBus 的去重 key 数量不再随进程生命周期无限增长。"
    - "事件仍在 recent-events 环内时，同 key 重复发布继续返回 false。"
    - "事件离开 recent-events 环后，其 key 可被回收并允许新的同 key 事件进入。"
  artifacts:
    - path: core/messages/bus.py
      provides: "与事件环同步的 dedupe key 淘汰"
    - path: tests/test_message_event_bus.py
      provides: "窗口内去重与窗口外回收回归"
---

<objective>
消除 `MessageEventBus._seen_dedupe_keys` 的无界增长，同时保持现有窗口内去重行为。

Purpose: 先处理无公开接口变化、无跨模块迁移的最低影响稳定性问题。
Output: 一个局部实现改动和一个可运行的回归测试。
</objective>

<tasks>
<task id="24-01-01" type="auto" tdd="true">
  <name>让 dedupe key 随 recent-events 环同步淘汰</name>
  <files>core/messages/bus.py, tests/test_message_event_bus.py</files>
  <behavior>
    - 窗口内相同 dedupe key 的第二次 publish 返回 false。
    - append 新事件即将淘汰最老事件时，同时移除该事件的 dedupe key。
    - 被淘汰 key 后续可重新 publish，recent-events 长度仍受 max_events 限制。
  </behavior>
  <action>在现有 deque append seam 内完成同步淘汰，不增加 cache class、TTL、定时任务或第三方依赖。</action>
  <verify><automated>python3 -m pytest tests/test_message_event_bus.py -q</automated></verify>
  <done>定向测试和 git diff check 通过。</done>
</task>
</tasks>

<verification>
- `python3 -m pytest tests/test_message_event_bus.py -q`
- `git diff --check`
</verification>

<success_criteria>
- 去重集合只保存 recent-events 环内事件的非空 key。
- 现有 MessageEventBus 公开接口和默认 `max_events=500` 不变。
- 不触碰 provider ingress、projection、UI、IPC 或持久化逻辑。
</success_criteria>
