---
phase: 24
plan: 03
title: 收敛 canonical publish、turn 顺序和前端 snapshot/delta 仲裁
type: execute
wave: 3
depends_on: [24-02]
requirements: [STAB-03]
files_modified:
  - core/messages/publishing.py
  - core/messages/session_bridge.py
  - core/messages/projections.py
  - bot/events.py
  - mac-app/src/App.tsx
  - mac-app/src/pages/TaskBoard.tsx
  - mac-app/src/components/menubar-popover/MenubarPopover.tsx
  - mac-app/src/components/session-browser/GenericProviderChat.tsx
  - mac-app/src/utils/sessionPolling.js
  - mac-app/src/utils/sessionPolling.d.ts
  - tests/test_message_event_bus.py
  - tests/test_events_streaming.py
  - mac-app/tests/appShell.test.mjs
  - mac-app/tests/menubarPopover.test.mjs
  - mac-app/tests/sessionPolling.test.mjs
  - mac-app/tests/providerSessionEventStream.test.mjs
autonomous: true
must_haves:
  truths:
    - "同一 canonical event 被 EventBus 拒绝为重复后，不再继续执行 Telegram/provider handler 副作用。"
    - "EventBus 不可用或发布异常仍不阻断既有 provider handler。"
    - "旧 turn 的迟到进度或终态不能覆盖当前 turn 的 session activity。"
    - "Task Board activity 只有 stream 一个前端写源。"
    - "Menubar 新 event 和 Session Chat live delta 不会被更早发出的 snapshot 请求回退。"
  artifacts:
    - path: core/messages/projections.py
      provides: "session-scoped turn identity guard"
    - path: bot/events.py
      provides: "canonical publish acceptance gate"
    - path: mac-app/src/App.tsx
      provides: "single Task Board activity stream authority"
    - path: mac-app/src/utils/sessionPolling.js
      provides: "generation-aware snapshot helper"
---

<objective>
让 canonical event、session activity 与前端 snapshot/delta 各自只沿一个可判定的新旧顺序更新状态。

Purpose: 修复重复 handler 副作用和迟到输入回退，不引入全局 store、跨进程 revision 或新依赖。
Output: 现有 publish/projection seam 的顺序守卫，以及前端本地 generation 仲裁。
</objective>

<tasks>
<task id="24-03-01" type="auto" tdd="true">
  <name>让 canonical publish 结果控制 handler 副作用</name>
  <files>core/messages/publishing.py, core/messages/session_bridge.py, bot/events.py, tests/test_message_event_bus.py, tests/test_events_streaming.py</files>
  <action>区分 accepted、duplicate 与 unavailable/error；只对确认 duplicate 的事件跳过 handler，并让无稳定身份的事件不参与 canonical dedupe。</action>
  <verify><automated>python3 -m pytest tests/test_message_event_bus.py tests/test_events_streaming.py -q</automated></verify>
  <done>重复事件只产生一次 bus 与 Telegram/provider 副作用，bus 故障继续走原 handler。</done>
</task>

<task id="24-03-02" type="auto" tdd="true">
  <name>在 projection shared seam 拒绝旧 turn 回退</name>
  <files>core/messages/projections.py, tests/test_message_event_bus.py</files>
  <action>记录 session 最近接受的 turn id；新 `turn.started` 切换所有权，其他 turn-scoped 事件只有匹配当前 turn 时才能修改 activity。</action>
  <verify><automated>python3 -m pytest tests/test_message_event_bus.py -q</automated></verify>
  <done>当前 turn 运行中或完成后，旧 turn 的 progress/terminal 都不能改写状态、消息和 lastEventKind。</done>
</task>

<task id="24-03-03" type="auto" tdd="false">
  <name>收敛 Task Board 与 Menubar 的前端状态写入顺序</name>
  <files>mac-app/src/App.tsx, mac-app/src/pages/TaskBoard.tsx, mac-app/src/components/menubar-popover/MenubarPopover.tsx, mac-app/tests/appShell.test.mjs, mac-app/tests/menubarPopover.test.mjs</files>
  <action>删除 Task Board 冗余 direct snapshot 写源；Menubar request response 只有在期间未收到更新 event 且时间不旧于当前值时才可提交。</action>
  <verify><automated>node --test tests/appShell.test.mjs tests/menubarPopover.test.mjs</automated></verify>
  <done>Task Board 只由 activity stream 写入，Menubar 旧 response 不覆盖新 event。</done>
</task>

<task id="24-03-04" type="auto" tdd="true">
  <name>丢弃 Session Chat live delta 之后返回的旧 snapshot</name>
  <files>mac-app/src/components/session-browser/GenericProviderChat.tsx, mac-app/src/utils/sessionPolling.js, mac-app/src/utils/sessionPolling.d.ts, mac-app/tests/sessionPolling.test.mjs, mac-app/tests/providerSessionEventStream.test.mjs</files>
  <action>复用一个本地 generation helper；session 切换、optimistic send 和 live delta 推进 generation，所有异步 snapshot 只在 generation 未变化时提交。</action>
  <verify><automated>node --test tests/sessionPolling.test.mjs tests/providerSessionEventStream.test.mjs</automated></verify>
  <done>旧 snapshot 被丢弃，同时保留现有 stream + polling fallback 行为。</done>
</task>
</tasks>

<verification>
- `/Users/wxy/.pyenv/versions/3.13.1/bin/python3 -m pytest tests/test_message_event_bus.py tests/test_events_streaming.py -q`
- `node --test tests/appShell.test.mjs tests/menubarPopover.test.mjs tests/sessionPolling.test.mjs tests/providerSessionEventStream.test.mjs`
- `git diff --check`
</verification>

<success_criteria>
- canonical duplicate 不再触发第二次 handler 副作用，publish failure 不阻断主链。
- 旧 turn 不能回退 projection 的当前状态。
- Task Board、Menubar 和 Session Chat 的已知 snapshot/delta 竞态都有最小回归。
- 不修改 provider source priority、stream/service handle、持久化或公开 IPC schema。
</success_criteria>
