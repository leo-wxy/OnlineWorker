---
phase: 24
plan: 02
title: 保留 last-known-good 并区分空结果与读取失败
type: execute
wave: 2
depends_on: [24-01]
requirements: [STAB-02]
files_modified:
  - mac-app/src-tauri/src/commands/task_board_state.rs
  - mac-app/src-tauri/src/menubar.rs
  - mac-app/src/pages/TaskBoard.tsx
  - mac-app/tests/appShell.test.mjs
  - mac-app/tests/menubarPopover.test.mjs
autonomous: true
must_haves:
  truths:
    - "owner bridge 成功返回空 activities 时，Task Board 和 Menubar 仍接受真实空结果。"
    - "owner bridge 缺失、超时或读取失败时，不再伪装为成功空结果。"
    - "Task Board 和 Menubar 在 activity 读取失败时保留最后一次有效状态。"
  artifacts:
    - path: mac-app/src-tauri/src/commands/task_board_state.rs
      provides: "明确区分空成功与 bridge 读取失败的 command"
    - path: mac-app/src/pages/TaskBoard.tsx
      provides: "失败时不覆盖共享 activity 状态的刷新逻辑"
    - path: mac-app/src-tauri/src/menubar.rs
      provides: "失败时不替换 Menubar snapshot store 的刷新逻辑"
---

<objective>
消除 activity 读取失败被折叠为空列表后覆盖有效 UI 状态的问题。

Purpose: 用现有 `Result`、`null` 哨兵和 snapshot store 完成最小修复，不增加第二套缓存。
Output: 三处失败传播修正和现有契约测试补强。
</objective>

<tasks>
<task id="24-02-01" type="auto" tdd="false">
  <name>让 owner bridge activity command 保留读取错误</name>
  <files>mac-app/src-tauri/src/commands/task_board_state.rs, mac-app/tests/appShell.test.mjs</files>
  <action>移除 socket 不存在时的成功空列表短路，统一交给现有 socket 读取函数返回错误；不改变成功响应中的真实空列表。</action>
  <verify><automated>node --test tests/appShell.test.mjs</automated></verify>
  <done>缺失 bridge 不再在 command 层伪装为空成功。</done>
</task>

<task id="24-02-02" type="auto" tdd="false">
  <name>失败时保留 Task Board 和 Menubar 的 last-known-good</name>
  <files>mac-app/src/pages/TaskBoard.tsx, mac-app/src-tauri/src/menubar.rs, mac-app/tests/appShell.test.mjs, mac-app/tests/menubarPopover.test.mjs</files>
  <action>Task Board activity 刷新失败返回现有 `null` 哨兵以跳过覆盖；Menubar activity 读取失败直接传播，使 snapshot store 保持不变。</action>
  <verify><automated>node --test tests/appShell.test.mjs tests/menubarPopover.test.mjs</automated></verify>
  <done>失败不清空有效状态，成功空结果仍可提交。</done>
</task>
</tasks>

<verification>
- `node --test tests/appShell.test.mjs tests/menubarPopover.test.mjs`
- `cargo test --manifest-path src-tauri/Cargo.toml task_board_state::tests --quiet`
- `cargo test --manifest-path src-tauri/Cargo.toml menubar::tests --quiet`
- `git diff --check`
</verification>

<success_criteria>
- transport/read failure 与成功空列表沿不同控制流传播。
- Task Board 和 Menubar activity 读取失败不覆盖最后一次有效状态。
- 不触碰 provider ingress、持久化、公开 contract 或 snapshot/delta generation 仲裁。
</success_criteria>
