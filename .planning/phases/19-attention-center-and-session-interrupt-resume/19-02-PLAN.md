---
phase: 19
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - mac-app/src/utils/taskBoard.js
  - mac-app/src/utils/taskBoard.d.ts
  - mac-app/src/pages/TaskBoard.tsx
  - mac-app/src/App.tsx
  - mac-app/src/pages/SessionBrowser.tsx
  - mac-app/src/components/session-browser/GenericProviderChat.tsx
  - mac-app/src/components/session-browser/shared.tsx
  - mac-app/tests/taskBoard.test.mjs
  - mac-app/tests/appShell.test.mjs
autonomous: true
requirements: [ATTN-01, SESS-CTRL-01]
---

<objective>
Redesign the existing Task Board into the selected compact grouped-list plus right detail pane and connect authority-safe lifecycle actions, including same-Session Continue with composer focus.
</objective>

<threat_model>
- T-19-03 False success: transient `interrupting`/`recovering` UI state must not overwrite provider activity truth.
- T-19-04 Accidental command: row selection and Continue must not send a message or trigger approval/control actions.
- T-19-05 Action leakage: mirrored-only rows must remain visible but expose no owned action controls.
</threat_model>

<tasks>

<task type="auto">
  <name>19-02-01 Build grouped attention-center model</name>
  <files>mac-app/tests/taskBoard.test.mjs, mac-app/src/utils/taskBoard.js, mac-app/src/utils/taskBoard.d.ts</files>
  <read_first>19-UI-SPEC.md, mac-app/src/utils/taskBoard.js, mac-app/tests/taskBoard.test.mjs</read_first>
  <action>Write failing Node tests for `需要你`, `正在运行`, and `最近结束` membership, owned-action priority, oldest-waiting ordering, mirrored-only action omission, and interrupted Continue. Then evolve the existing view model with no parallel store.</action>
  <acceptance_criteria>Every activity appears in at most one primary group; ordering follows D-07/D-08; controls come only from normalized capability facts.</acceptance_criteria>
  <verify><automated>node --test mac-app/tests/taskBoard.test.mjs</automated></verify>
</task>

<task type="auto">
  <name>19-02-02 Implement B+A Task Board and same-Session Continue focus</name>
  <files>mac-app/src/pages/TaskBoard.tsx, mac-app/src/App.tsx, mac-app/src/pages/SessionBrowser.tsx, mac-app/src/components/session-browser/GenericProviderChat.tsx, mac-app/src/components/session-browser/shared.tsx, mac-app/tests/appShell.test.mjs</files>
  <read_first>19-UI-SPEC.md, mac-app/src/pages/TaskBoard.tsx, mac-app/src/App.tsx, mac-app/src/pages/SessionBrowser.tsx, mac-app/src/components/session-browser/shared.tsx</read_first>
  <action>Write failing static/behavior tests for the grouped split layout, lifecycle command invocation, mirrored-only copy, responsive list/detail switch, and one-shot composer focus intent. Reshape the existing Task Board row/lane presentation, preserve stream/badge/pin/approval flows, add transient action state and inline errors, and thread a focus token through the existing Session open target. Continue navigates only; it never populates or submits the composer.</action>
  <acceptance_criteria>The existing Task Board route renders the approved B+A hierarchy, keyboard/aria states are explicit, real controls call the new Tauri command, and Continue focuses an empty existing composer for the same Session.</acceptance_criteria>
  <verify><automated>node --test mac-app/tests/taskBoard.test.mjs mac-app/tests/appShell.test.mjs &amp;&amp; pnpm --dir mac-app build</automated></verify>
</task>

</tasks>

<verification>
Run the complete Node Task Board/App/Session Browser suites, production frontend build, and `git diff --check`. Source verification does not claim packaged visual or live-provider UAT.
</verification>

<success_criteria>
- Existing Task Board Tab uses compact grouped rows with a selected detail pane.
- Actions are present only when the normalized owned capability exists.
- User interruption lands in recent-ended with Continue; unexpected failure remains actionable.
- Continue opens the same Session and focuses the composer without sending generated text.
- Existing approval authority, badge, stream, pin, and Session navigation behavior remain intact.
</success_criteria>

## Artifacts this phase produces

- Attention-center Task Board view model and B+A presentation.
- Authority-safe interrupt/recovery action UI.
- Same-Session Continue composer-focus contract.
