---
phase: 19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/messages/events.py
  - core/messages/projections.py
  - core/provider_owner_bridge.py
  - mac-app/src-tauri/src/commands/task_board_state.rs
  - mac-app/src-tauri/src/lib.rs
  - mac-app/src-tauri/src/menubar.rs
  - tests/test_message_event_bus.py
  - tests/test_provider_owner_bridge.py
autonomous: true
requirements: [SESS-CTRL-01]
---

<objective>
Expose provider-owned Session interrupt/recovery through the existing Task Board bridge and classify user interruption from authoritative provider events without creating local Session truth.
</objective>

<threat_model>
- T-19-01 Authority confusion: mirrored-only or externally controlled Sessions must fail closed and never dispatch a control hook.
- T-19-02 Replay/duplication: recovery must never resend the prior user message or silently create a replacement Session.
- T-19-03 False success: UI/transport results must distinguish request acceptance from provider-confirmed terminal/recovered state.
</threat_model>

<tasks>

<task type="auto">
  <name>19-01-01 Classify authoritative user interruption</name>
  <files>tests/test_message_event_bus.py, core/messages/projections.py</files>
  <read_first>19-CONTEXT.md, core/messages/session_bridge.py, core/messages/projections.py</read_first>
  <action>Write a failing projection test for aborted/cancelled user interruption, then minimally classify it as a recent interrupted terminal state with a Continue affordance signal. Keep unexpected failure in `needs_attention`.</action>
  <acceptance_criteria>User interruption and unexpected failure produce distinct normalized activity facts; inactivity is never interruption.</acceptance_criteria>
  <verify><automated>pytest -q tests/test_message_event_bus.py -k interrupt</automated></verify>
</task>

<task type="auto">
  <name>19-01-02 Add ownership-safe owner-bridge Session controls</name>
  <files>tests/test_provider_owner_bridge.py, core/provider_owner_bridge.py</files>
  <read_first>19-CONTEXT.md, core/providers/contracts.py, bot/thread_controls.py, plugins/providers/builtin/codex/python/runtime.py, plugins/providers/builtin/claude/python/adapter.py</read_first>
  <action>Write failing tests for supported interrupt, mirrored/external rejection, missing active-turn rejection, and provider-owned recovery without replay. Add normalized control availability to Task Board activities and a `session_control` request handler that dispatches only through an existing owned provider adapter/hook. Return explicit accepted/unsupported/error results and wait for subsequent provider events to change activity truth.</action>
  <acceptance_criteria>No control path materializes placeholder state; unsupported and mirrored-only cases invoke no provider action; recovery never sends content.</acceptance_criteria>
  <verify><automated>pytest -q tests/test_provider_owner_bridge.py -k session_control</automated></verify>
</task>

<task type="auto">
  <name>19-01-03 Add Tauri Task Board control transport</name>
  <files>mac-app/src-tauri/src/commands/task_board_state.rs, mac-app/src-tauri/src/lib.rs</files>
  <read_first>19-CONTEXT.md, mac-app/src-tauri/src/commands/task_board_state.rs, mac-app/src-tauri/src/lib.rs</read_first>
  <action>Add Rust tests first for activity control fields and the `session_control` request/result payload. Then expose one command that forwards interrupt/recover and preserves explicit provider reasons.</action>
  <acceptance_criteria>React receives normalized capability/reason fields and can request controls without provider-private parsing.</acceptance_criteria>
  <verify><automated>cargo test --manifest-path mac-app/src-tauri/Cargo.toml task_board_state --quiet</automated></verify>
</task>

</tasks>

<verification>
Run the Python projection/bridge suites and Rust Task Board suite. Inspect the diff for any new control path that bypasses provider ownership or app-server approval authority.
</verification>

<success_criteria>
- Provider-owned interrupt/recovery requests are available through one normalized bridge command.
- Mirrored-only, missing-turn, and unsupported controls fail closed.
- User interruption is not conflated with unexpected failure.
- No click/timeout is persisted as authoritative Session success.
</success_criteria>

## Artifacts this phase produces

- Normalized Session control availability and result contract.
- Owner-bridge interrupt/recovery command path.
- Authoritative interrupted terminal projection.
