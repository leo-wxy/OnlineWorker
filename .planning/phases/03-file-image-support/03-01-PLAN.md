---
phase: 03-file-image-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/providers/contracts.py
  - core/providers/message_runtime.py
  - bot/handlers/message.py
  - bot/handlers/thread.py
  - core/provider_session_bridge.py
  - plugins/providers/builtin/codex/plugin.yaml
  - plugins/providers/builtin/codex/python/provider.py
  - plugins/providers/builtin/codex/python/runtime.py
  - plugins/providers/builtin/codex/python/adapter.py
  - plugins/providers/builtin/claude/plugin.yaml
  - plugins/providers/builtin/claude/python/provider.py
  - plugins/providers/builtin/claude/python/runtime.py
  - plugins/providers/builtin/claude/python/adapter.py
  - tests/test_handlers.py
  - tests/test_thread_controls.py
  - tests/test_config.py
requirements:
  - ATT-01
  - ATT-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - Telegram 图片和通用文件都能进入统一 attachment message contract
    - provider-specific 附件处理仍然保持在 provider/runtime 边界
    - builtin codex / claude 的公开能力声明与真实运行时行为一致
  artifacts:
    - 统一 attachment message contract
    - Telegram 附件接收与 provider send 链路
---

<objective>
升级共享消息 contract，并打通 Telegram → 本地落地 → provider runtime 的附件发送链路，覆盖图片和通用文件两类输入。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 定义统一附件消息 contract 与能力声明</name>
  <files>core/providers/contracts.py, core/providers/message_runtime.py, plugins/providers/builtin/codex/plugin.yaml, plugins/providers/builtin/claude/plugin.yaml, plugins/providers/builtin/codex/python/provider.py, plugins/providers/builtin/claude/python/provider.py</files>
  <action>把当前“text + has_photo + supports_photo/photos”升级为可表达 text + attachments、image/file 能力位的共享 contract，并同步 builtin provider manifest / descriptor。</action>
  <verify>rg -n "attachment|supports_file|supports_image|attachments" core/providers plugins/providers/builtin</verify>
</task>

<task type="auto">
  <name>Task 2: 打通 Telegram 图片与文件的本地落地和 thread send</name>
  <files>bot/handlers/message.py, bot/handlers/thread.py, core/provider_session_bridge.py</files>
  <action>让 Telegram handler 识别 `photo` 与 `document`，下载到受控目录，构造统一消息 payload，并复用现有 thread/workspace 路由把附件送进 provider 流程。</action>
  <verify>python -m pytest -q tests/test_handlers.py tests/test_thread_controls.py</verify>
</task>

<task type="auto">
  <name>Task 3: 为 builtin codex / claude 实现附件发送适配</name>
  <files>plugins/providers/builtin/codex/python/runtime.py, plugins/providers/builtin/codex/python/adapter.py, plugins/providers/builtin/claude/python/runtime.py, plugins/providers/builtin/claude/python/adapter.py</files>
  <action>保持共享层 provider-neutral；在 builtin provider runtime / adapter 内处理附件如何转成各自 CLI 可接受的输入形式，并在不支持场景下返回明确错误。</action>
  <verify>python -m pytest -q tests/test_handlers.py tests/test_config.py</verify>
</task>

</tasks>

<verification>
- [ ] `python -m pytest -q tests/test_handlers.py tests/test_thread_controls.py tests/test_config.py`
</verification>

<output>
After completion, create `.planning/phases/03-file-image-support/03-01-SUMMARY.md`
</output>
