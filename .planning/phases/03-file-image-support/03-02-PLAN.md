---
phase: 03-file-image-support
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - mac-app/src/components/session-browser/shared.tsx
  - mac-app/src/pages/SessionBrowser.tsx
  - mac-app/src/components/session-browser/api.ts
  - mac-app/src/types.ts
  - mac-app/src-tauri/src/commands/provider_sessions.rs
  - mac-app/src-tauri/src/lib.rs
  - mac-app/tests/appShell.test.mjs
  - mac-app/package.json
  - scripts/build.sh
requirements:
  - ATT-01
  - ATT-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - 桌面端 session composer 能真实选择并发送图片/文件
    - 桌面端附件发送仍然经过 Tauri bridge 与 provider/plugin 边界
    - 打包安装后的 OnlineWorker.app 仍能启动并验证附件工作流
  artifacts:
    - SessionBrowser 附件入口
    - Tauri 附件发送命令
    - 安装态验证记录
---

<objective>
打通桌面端附件发送入口，并完成打包安装后的附件工作流验证，确保附件支持不是源码态专用功能。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 让 SessionComposer 的附件入口真正可用</name>
  <files>mac-app/src/components/session-browser/shared.tsx, mac-app/src/pages/SessionBrowser.tsx, mac-app/src/types.ts</files>
  <action>为 composer 增加文件/图片选择、已选附件状态和发送时的 payload 组装；保持现有 thread 发送交互，不新建独立附件页面。</action>
  <verify>node --test mac-app/tests/appShell.test.mjs</verify>
</task>

<task type="auto">
  <name>Task 2: 扩展 Tauri provider session 发送桥接</name>
  <files>mac-app/src/components/session-browser/api.ts, mac-app/src-tauri/src/commands/provider_sessions.rs, mac-app/src-tauri/src/lib.rs</files>
  <action>让桌面端可以把本地附件路径与文本一起通过 Tauri invoke 送入 provider session send 路径，不把本地路径解析逻辑泄漏到 React 页面之外。</action>
  <verify>cd mac-app && npm run build</verify>
</task>

<task type="auto">
  <name>Task 3: 重跑打包安装验证附件能力</name>
  <files>scripts/build.sh, mac-app/package.json</files>
  <action>如果 Python sidecar 或桌面端桥接被改动，重建 sidecar、重打包、覆盖安装 `/Applications/OnlineWorker.app`，验证安装态能启动，并至少完成一轮 Telegram 附件发送和桌面端附件发送冒烟。</action>
  <verify>bash scripts/build.sh</verify>
</task>

</tasks>

<verification>
- [ ] `node --test mac-app/tests/appShell.test.mjs`
- [ ] `cd mac-app && npm run build`
- [ ] `bash scripts/build.sh`
</verification>

<output>
After completion, create `.planning/phases/03-file-image-support/03-02-SUMMARY.md`
</output>
