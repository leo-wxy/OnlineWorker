---
phase: 02-provider-usage-explorer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mac-app/src-tauri/src/commands/provider_usage.rs
  - mac-app/src-tauri/src/commands/mod.rs
  - mac-app/src-tauri/src/lib.rs
  - mac-app/src/types.ts
  - mac-app/src/components/session-browser/api.ts
requirements:
  - USG-01
  - USG-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - Usage 数据通过统一 contract 暴露给前端
    - Codex 和 Claude 的解析逻辑留在 provider 命令层
    - 每日用量按本地 provider 数据源聚合，而不是写死假数据
  artifacts:
    - mac-app/src-tauri/src/commands/provider_usage.rs
  key_links:
    - get_provider_usage_summary
---

<objective>
定义 provider usage contract，并在 Tauri 命令层实现 `Codex / Claude` 的本地 daily usage 读取与聚合。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 定义统一 summary contract</name>
  <files>mac-app/src-tauri/src/commands/provider_usage.rs, mac-app/src/types.ts, mac-app/src/components/session-browser/api.ts</files>
  <action>新增 `ProviderUsageDay` / `ProviderUsageSummary`，并暴露 `fetchProviderUsageSummary()` 给前端。</action>
  <verify>rg -n "ProviderUsageSummary|fetchProviderUsageSummary|get_provider_usage_summary" mac-app/src mac-app/src-tauri/src</verify>
</task>

<task type="auto">
  <name>Task 2: 实现 Codex 按日聚合</name>
  <files>mac-app/src-tauri/src/commands/provider_usage.rs</files>
  <action>从 `~/.codex/sessions/**/*.jsonl` 读取 `token_count` 事件，优先 `last_token_usage`，否则使用累计值 delta。</action>
  <verify>cargo test --manifest-path mac-app/src-tauri/Cargo.toml provider_usage --quiet</verify>
</task>

<task type="auto">
  <name>Task 3: 实现 Claude 按日聚合与去重</name>
  <files>mac-app/src-tauri/src/commands/provider_usage.rs</files>
  <action>从 `~/.claude/projects/**/*.jsonl` 读取 `message.usage`，以 `message.id + requestId` 去重后按日期聚合。</action>
  <verify>cargo test --manifest-path mac-app/src-tauri/Cargo.toml provider_usage --quiet</verify>
</task>

</tasks>

<verification>
- [x] `cargo test --manifest-path mac-app/src-tauri/Cargo.toml provider_usage --quiet`
</verification>

<output>
After completion, create `.planning/phases/02-provider-usage-explorer/02-01-SUMMARY.md`
</output>
