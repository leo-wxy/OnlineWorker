---
phase: 01-ui-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mac-app/src/App.tsx
requirements:
  - UI-01
  - UI-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - 左侧导航栏可以在展开态和窄栏态之间切换
    - 收缩后的导航仍可在主要 tab 之间高效切换
    - 收缩态不会造成导航内容重叠或主内容区布局错乱
  artifacts:
    - mac-app/src/App.tsx
  key_links:
    - sidebarCollapsed 状态驱动 sidebar 宽度与导航项显隐
---

<objective>
在应用壳层里实现左侧导航栏的收缩 / 展开能力，并确保收缩态保留窄栏而非完全隐藏。

Purpose: 先解决用户直接感知到的壳层效率问题，为后续 `Setup`、`Dashboard`、`Sessions`、`Commands` 的视觉统一打基础。
Output: 支持窄栏切换的顶层导航壳层实现。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-foundation/01-CONTEXT.md
@.planning/phases/01-ui-foundation/01-RESEARCH.md
@mac-app/src/App.tsx
@mac-app/src/utils/appTabs.js
@mac-app/src/i18n/locales/en.ts
@mac-app/src/i18n/locales/zh.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 sidebar 增加收缩状态和切换入口</name>
  <files>mac-app/src/App.tsx</files>
  <read_first>mac-app/src/App.tsx, .planning/phases/01-ui-foundation/01-CONTEXT.md</read_first>
  <action>在 `App.tsx` 顶层壳层中新增 `sidebarCollapsed` 本地状态。为 sidebar 增加一个明确的收缩/展开按钮，并让按钮只控制应用壳层自身，不引入新的全局状态或跨页面依赖。保持当前 `activeTab`、`showLogs`、`isFirstRun` 这些既有状态模式不变。</action>
  <verify>rg -n "sidebarCollapsed|setSidebarCollapsed" mac-app/src/App.tsx</verify>
  <acceptance_criteria>
    - `App.tsx` 中存在 `sidebarCollapsed` 状态定义
    - `App.tsx` 中存在收缩/展开按钮点击逻辑
  </acceptance_criteria>
  <done>`App.tsx` 已能在代码层驱动 sidebar 展开/收缩切换</done>
</task>

<task type="auto">
  <name>Task 2: 调整导航项与辅助块的收缩态行为</name>
  <files>mac-app/src/App.tsx</files>
  <read_first>mac-app/src/App.tsx, .planning/phases/01-ui-foundation/01-RESEARCH.md</read_first>
  <action>让 sidebar 在展开态保持现有图标+文字结构，在收缩态切换为 icon-first 窄栏。导航按钮在收缩态仍保持足够点击面积，并对品牌区、语言切换和底部服务状态块做收缩态显隐处理，避免文本在窄栏内挤压或截断。不要改动各页面组件的业务逻辑。</action>
  <verify>rg -n "PRIMARY_APP_TABS|locale|Service Active|Local AI workbench" mac-app/src/App.tsx</verify>
  <acceptance_criteria>
    - 导航按钮在收缩态不再依赖完整文字展示
    - 低频辅助块在收缩态不会制造明显布局噪音
    - 主内容区结构仍保持 `flex-1` 主体布局
  </acceptance_criteria>
  <done>收缩态导航、品牌区和辅助块行为已定义完整</done>
</task>

<task type="auto">
  <name>Task 3: 做最小验证，确认壳层切换不破坏构建</name>
  <files>mac-app/src/App.tsx</files>
  <read_first>mac-app/package.json</read_first>
  <action>完成实现后运行前端构建或类型检查，验证 `App.tsx` 的状态与条件渲染调整没有引入 TS/构建错误。若需要，补最小静态检查确保 sidebar 切换逻辑可读且不破坏现有 tab 跳转。</action>
  <verify>cd mac-app && npm run build</verify>
  <acceptance_criteria>
    - 前端 build 通过
    - 没有因 sidebar 状态调整引入新的类型错误
  </acceptance_criteria>
  <done>壳层收缩功能至少达到可构建基线</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd mac-app && npm run build` 通过
- [ ] `App.tsx` 中存在明确的 sidebar 收缩态状态与切换逻辑
- [ ] 收缩态设计仍满足“窄栏，不隐藏”的用户约束
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 左侧导航栏支持展开 / 窄栏两种稳定状态
- 主导航切换效率不因收缩态下降到不可用

</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-foundation/01-01-SUMMARY.md`
</output>
